# Local development settings. Gitignored — never committed, never deployed.
# Production values are set as environment variables on DigitalOcean instead
# (see docs/DEPLOY-DIGITALOCEAN.md).

# Build the blog from src/data/blog-fixtures.json instead of the admin API,
# so `npm run build` and `npm run dev` work before the admin exists.
# Delete this line once BLOG_API_URL below is real.
BLOG_ALLOW_FIXTURES=1

# Uncomment and fill in once the admin endpoints exist:
# BLOG_API_URL=https://admin.speakup.al/api/posts
# BLOG_API_TOKEN=
# PUBLIC_CONTACT_API_URL=https://admin.speakup.al/api/enquiries
# PUBLIC_BOOKING_API_URL=https://admin.speakup.al/api/booking

SITE_URL=http://localhost:4321
