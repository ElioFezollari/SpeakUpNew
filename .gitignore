# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.production
.env.local
# Hand-made copies of the above. `.env` does not match these, and one of them
# committed by a `git add -A` is a secret in the history.
.env.backup*
.env.bak

# logs
npm-debug.log*
yarn-error.log*
*.log

# editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# design handoff bundle (reference only, not part of the build)
_mockups/
