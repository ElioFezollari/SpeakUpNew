---
// Albanian clinic tour, served at `/klinika/`.
import Base from '../../layouts/Base.astro';
import ClinicPage from '../../components/ClinicPage.astro';
import { useCopy } from '../../i18n/utils';

const { t } = useCopy(Astro.currentLocale);
---

<Base title={t.clinic.title} description={t.clinic.lede}>
  <ClinicPage locale="sq" />
</Base>
