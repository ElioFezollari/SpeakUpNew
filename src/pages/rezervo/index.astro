---
// Albanian booking page, served at `/rezervo/`.
import Base from '../../layouts/Base.astro';
import BookingPage from '../../components/BookingPage.astro';
import { useCopy } from '../../i18n/utils';

const { t } = useCopy(Astro.currentLocale);
---

<Base title={t.booking.title} description={t.booking.lede}>
  <BookingPage locale="sq" />
</Base>
